import React from "react";
import logoImage from "../assets/images/logo/logo.svg";

const Logo = () => {
  return <img src={logoImage} alt="logo image" width={90} height={90} />;
};

export default Logo;
